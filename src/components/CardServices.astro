---
import Imagen1 from "../../public/img/Service1.webp"

const {imagen=Imagen1,alt="services", titulo="Titulo", descripcion="Descripcion"} = Astro.props
---
<div class="w-[340px] h-[620px] rounded-2xl flex flex-col gap-3 border border-slate-300/30 shadow-xl shadow-gray-300">
    <div class="w-full h-full max-h-80 overflow-hidden rounded-t-2xl">
        <img src={imagen.src} class="w-full h-full object-cover" alt={alt}>
    </div>
    <div class="px-5 flex flex-col gap-3 w-full h-[195px]">
        <h2 class="h-max text-azulLogo font-dosis font-bold text-2xl">{titulo}</h2>
        <p class="font-nunito text-base">{descripcion}</p>
    </div>
    <div class="px-5 flex items-center justify-center">
        <button class="bg-azulLogo px-3 py-5 text-white w-full rounded-xl flex justify-center">
            Ver más...
        </button>
    </div>
    
</div>